import { PagePlaceholder } from "@/components/dashboard/page-placeholder";

export default function WeeklyReviewPage() {
  return (
    <PagePlaceholder
      title="Weekly Review"
      description="Summarize completed tasks, habit consistency, goal progress, notes, wins, gaps, and next-week suggestions."
    />
  );
}
